---

# jenkins variables
project_name: docker-xemu
external_type: github_stable
release_type: stable
release_tag: latest
ls_branch: master
build_armhf: false
repo_vars:
  - EXT_GIT_BRANCH = 'master'
  - EXT_USER = 'xemu-project'
  - EXT_REPO = 'xemu'
  - BUILD_VERSION_ARG = 'XEMU_VERSION'
  - LS_USER = 'linuxserver'
  - LS_REPO = 'docker-xemu'
  - CONTAINER_NAME = 'xemu'
  - DOCKERHUB_IMAGE = 'linuxserver/xemu'
  - DEV_DOCKERHUB_IMAGE = 'lsiodev/xemu'
  - PR_DOCKERHUB_IMAGE = 'lspipepr/xemu'
  - DIST_IMAGE = 'ubuntu'
  - MULTIARCH = 'true'
  - CI = 'true'
  - CI_WEB = 'true'
  - CI_PORT = '3001'
  - CI_SSL = 'true'
  - CI_DELAY = '120'
  - CI_DOCKERENV = 'TZ=US/Pacific'
  - CI_AUTH = 'user:password'
  - CI_WEBPATH = ''
